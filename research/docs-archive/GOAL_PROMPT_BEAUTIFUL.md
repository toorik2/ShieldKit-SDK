# Goal prompt: Beautiful ShieldKit (execute plan to done)

Paste as one message (or `/goal`):

```text
/goal Execute docs/BEAUTIFUL_PLAN.md to completion. Inner beauty first: bold merge/rename/delete toward simplicity — not facades over sprawl. Ship one spine, dual setup modes, four user verbs.

## Binding
- Plan authority: docs/BEAUTIFUL_PLAN.md (supersedes DEVKIT_POLISH_PLAN ceremony-park / no-rename framing).
- Also: AGENTS.md, CHARTER, KILL_GATES, PRIVACY, SECURITY, policy/g0-lock.json.
- Never commit secrets, HANDOVER, WIFs, seeds, battery state, real mainnet broadcast without explicit user auth + flags.
- Default network Chipnet. Fee+change 1 sat/B. Unlock ≤10k B; settlement ≤59k B. PF7 topology fixed. No hot-swap: new setup ⇒ new profile + new genesis.

## Product end-state
1. **Packages ≤6 product domains:** kit · profile · action · prove · recover (+ optional mobile). Merge/delete the ~25 peer packages into those homes. Gate IDs (g2-*, etc.) out of public names/exports.
2. **One facade:** createKit only (kill app-kit vs sdk dualism). Scope @shieldkit/* for product packages.
3. **One CLI:** scripts/shieldkit.mjs subcommands: init | deposit | transfer | withdraw | recover | doctor.
4. **profile.init modes:** development-only | ceremony-production — same pipeline setup→build→genesis→load. Ceremony is real runner (kernel + ≥2 contribs + transcript), not schema-only. Mode laundering refused.
5. **prove:** groth16 + PF7 unlocks as in-tree library path (not permanent dens-drop worktree folklore).
6. **README** teaches only the four verbs + honesty banner. Docs: one architecture story (this plan); archive/supersede conflicting polish goals.

## Non-goals (reject)
Hosted ceremony SaaS; auto fund migration; wallet keystore shell; multi-note expansion; G9/release theater because ceremony code exists; mainnet volume; new circuits/VK topology; generic ZK framework.

## Execution order
A) Freeze public API + full old→new package map + deletion list.
B) Move code (profile → action → prove → recover → kit); implement ceremony on shared setup kernel; single createKit + CLI verbs.
C) User pass: README, doctor, tiny 2-party ceremony→profile offline, optional pinned dev demo path.
D) Verify: npm test green; red-team real APIs; dual-mode load same loader; one act path no legacy peer tourism; mainnet+development-only still refuse without lab override.

## Done when (all true)
- Napkin architecture = profile/action/prove/recover/kit only.
- App path: init(dev) → deposit/transfer/withdraw → recover without research packages.
- Operator path: init(ceremony) → same kit load/act.
- No dual facades; no setup four-step tourism; no second stack.
- Claims honest; secrets clean; tests + red-team pass.

Work until Done. Prefer delete over re-export forever. Be bold for elegance.
```
