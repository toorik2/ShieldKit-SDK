pragma cashscript ^0.13.0;
contract SZ() {
    function spend(int a0,int a1,int a2,int a3,int a4,int a5,int a6,int a7,int a8,int a9,int a10,int a11, int b0,int b1,int b2,int b3,int b4,int b5,int b6,int b7,int b8,int b9,int b10,int b11, int c0,int c1,int c2,int c3,int c4,int c5,int c6,int c7,int c8,int c9,int c10,int c11, int q0,int q1,int q2,int q3,int q4,int q5,int q6,int q7,int q8,int q9,int q10, int r) {
        int P = 21888242871839275222246405745257275088696311157297823662689037894645226208583;
        int ar = a11;
        ar = (ar * r + a10) % P;
        ar = (ar * r + a9) % P;
        ar = (ar * r + a8) % P;
        ar = (ar * r + a7) % P;
        ar = (ar * r + a6) % P;
        ar = (ar * r + a5) % P;
        ar = (ar * r + a4) % P;
        ar = (ar * r + a3) % P;
        ar = (ar * r + a2) % P;
        ar = (ar * r + a1) % P;
        ar = (ar * r + a0) % P;
        int br = b11;
        br = (br * r + b10) % P;
        br = (br * r + b9) % P;
        br = (br * r + b8) % P;
        br = (br * r + b7) % P;
        br = (br * r + b6) % P;
        br = (br * r + b5) % P;
        br = (br * r + b4) % P;
        br = (br * r + b3) % P;
        br = (br * r + b2) % P;
        br = (br * r + b1) % P;
        br = (br * r + b0) % P;
        int cr = c11;
        cr = (cr * r + c10) % P;
        cr = (cr * r + c9) % P;
        cr = (cr * r + c8) % P;
        cr = (cr * r + c7) % P;
        cr = (cr * r + c6) % P;
        cr = (cr * r + c5) % P;
        cr = (cr * r + c4) % P;
        cr = (cr * r + c3) % P;
        cr = (cr * r + c2) % P;
        cr = (cr * r + c1) % P;
        cr = (cr * r + c0) % P;
        int qr = q10;
        qr = (qr * r + q9) % P;
        qr = (qr * r + q8) % P;
        qr = (qr * r + q7) % P;
        qr = (qr * r + q6) % P;
        qr = (qr * r + q5) % P;
        qr = (qr * r + q4) % P;
        qr = (qr * r + q3) % P;
        qr = (qr * r + q2) % P;
        qr = (qr * r + q1) % P;
        qr = (qr * r + q0) % P;

        int r2 = (r * r) % P; int r3 = (r2 * r) % P; int r6 = (r3 * r3) % P; int r12 = (r6 * r6) % P;
        int Mr = (r12 + 82 + 18 * P - (18 * r6) % P) % P;
        require((ar * br) % P == (qr * Mr + cr) % P);
    }
}
