# verifier.cash keeps SOURCE in git (incl. the pinned cashc fork source in vendor/cashc-resched, so the
# compiler can never evaporate) but NOT rebuildable toolchain bulk. Run ./setup.sh after cloning to
# regenerate node_modules + the cashc dist.

# Rebuildable toolchain outputs (regenerated by setup.sh) — do NOT commit
node_modules/
node_modules
vendor/**/dist/
vendor/**/dist
.lake/

# Ephemeral
*.log
*.bak
out/
.vc/
scratchpad_*.mjs
scratchpad/
.claude/
.DS_Store

# Stale top-level layout from before the 2026-07-10 consolidation (now under build/ and harness/)
/groth16_cashscript/
/zk-verifier-bench/
/cashscript/
