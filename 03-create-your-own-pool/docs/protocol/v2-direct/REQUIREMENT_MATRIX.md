# ShieldKit Protocol V2 Direct — Requirement Matrix

**Status date:** 2026-07-31
**Scope:** `03-create-your-own-pool/` in branch `feat/v2-direct-20260728`
**Authority:** `IMPLEMENTATION_PLAN.md`; this matrix is an execution tracker, not a qualification claim.

## Reading this matrix

`Missing` means the required V2 Direct scaffold is absent, or only a legacy
component exists. `Partial` means a V2 Direct local scaffold/test source exists but
the listed requirement also needs missing integration, immutable artifacts, or
qualification evidence. `Existing` means a current repository component supplies
the stated *scaffold*, never that it satisfies the V2 requirement unless the
evidence column says so. The V2 Direct sources and documents listed here are
tracked development-only inputs; `.codex-build/` is ignored local
development-artifact storage. Source tracking alone passes or qualifies no row.

The exact intended paths below are deliberate ownership boundaries for the V2 work;
they do not authorize substituting the similarly named V1 modules. Evidence artifacts
are profile-specific and must be immutable, signed where required, and referenced by
their SHA-256 from the final instance descriptor.

### Inventory and baseline evidence

| Observation | Concrete evidence | Consequence |
|---|---|---|
| The active V2 documents are tracked development inputs. | `git ls-files 03-create-your-own-pool/docs/protocol/v2-direct/` lists the plan, matrix, layouts, domains, privacy policy, topology prohibitions, and verifier decision. | Tracked planning documents are not an immutable reviewed specification or released protocol. |
| V2 implementation and tests are tracked, but remain development-only inputs. | V2 sources are tracked under `packages/{action,profile,kit,pool,unlock-builder}/v2/`, `circuits/`, and `crates/`. Current ignored local PF10/profile artifacts are present but are not frozen or release artifacts. | Do not infer a frozen relation, final artifacts, qualification, or verified local metric from source presence alone. |
| The current V2 identity is development-only. | `.codex-build/v2-pf10-libauth-qualification/libauth.json` is present and self-labels `eligibility: development-only`, `finalKey: false`, and `releaseQualified: false`; adapter-specific ignored directories remain noncanonical. | Current local evidence may document only its explicit development scope; it neither supersedes other evidence nor establishes a release descriptor. |
| The default portable suite and the local verifier lane have different scopes. | `run-domain-tests.mjs` classifies PF10/PairFold artifact tests as a local verifier lane; its provisioning and CI path use development-only local artifacts. | Portable/default results and local-lane results are regression/development evidence only; neither closes any external or release gate. |
| Reusable domains exist. | `packages/action`, `profile`, `prove`, `kit`, `pool`, and `recover` have source and test modules; `packages/profile/setup/` has development/ceremony tooling. | Reuse only behind V2-specific schemas and vectors; do not relabel outputs. |

## Protocol requirements

| ID | Normative requirement | Intended code / test / artifact location | Current status and concrete evidence | Dependencies and objective pass evidence |
|---|---|---|---|---|
| P-01 | One local witness, one local Groth16 proof, one wallet funding input, and one BCH settlement transaction; prohibit batching, coordinator, sponsor, remote prover, preparation transaction, recursion, and root-history accumulator. | `docs/protocol/v2-direct/PROHIBITED_TOPOLOGIES.md`; `packages/action/v2/{topology,settlement,circuit-witness}.mjs`; corresponding tests | **Partial, tracked development-only scaffold.** The V2 Direct topology policy/document and one-settlement construction paths exist; legacy preparation-era sources remain separately present. No real V2 BCH acceptance evidence closes this requirement. | Depends on P-02–P-13. Pass only with static topology tests and real three-action transactions proving exactly one settlement each. |
| P-02 | Encode/decode the exact 128-byte `SKS2` state: all offsets, raw-SHA-256 `profileId`, BE canonical roots, LE counters/amounts, and CashToken category byte order. | `packages/action/v2/{state,state.test,strict-codec-qualification,typescript/codec}.mjs`; `crates/shieldkit-v2-codec/{src/lib.rs,tests/vectors.rs}`; `docs/protocol/v2-direct/BYTE_LAYOUTS.md` | **Partial, locally exercised.** JS, independent TypeScript, and Rust codecs define `SKS2`/128-byte offsets and each completed the local strict sweep: 32,640 state one-byte replacements, 127/129-byte rejection, category byte-order, and SHA-256/u128 agreement. This is source-level, ignored-local evidence, not frozen four-implementation/covenant qualification. | Phase A. Pass with committed frozen cross-runtime/circuit/covenant vectors and independently replayable boundary evidence. |
| P-03 | Enforce the complete state invariant set: count/capacity/sequence bounds, `live = note - nullifier`, reserve equation, u32/u64 overflow preconditions, fixed denomination/depths, 210M qualified and 32-note playground capacity. | `packages/action/v2/{state,transition,transition.test,circuit-model}.mjs`; `circuits/v2-direct/{state,pool-action}.circom`; local test sources | **Partial, tracked development-only scaffold.** The V2 state/transition code and circuit sources encode the stated arithmetic shape. Exhaustive boundary artifacts, independent frozen-model evidence, and covenant/VM agreement are absent. | P-02 and V2 reference models. Pass with exhaustive boundary vectors plus independently implemented model agreement. |
| P-04 | Define `profileId = SHA256("SKP2" || RFC8785(profileCore))`; include and exclude exactly the specified fields; instance descriptor pins identity, genesis/outpoint, final locks/base values, state, and signed manifest. | `packages/profile/v2/{profile-core,instance-descriptor,genesis}.mjs`; corresponding tests; `docs/protocol/v2-direct/{IMPLEMENTATION_PLAN,IDS_AND_DOMAINS}.md` | **Partial, tracked development-only scaffold.** V2 profile-core canonicalization/identity and descriptor validation sources exist. They do not provide a frozen profile, signed final descriptor, or two-clean-host load evidence. | P-02, P-08, P-13, finalized artifacts. Pass with canonicalization/cycle-rejection vectors and signed-descriptor load on two clean hosts. |
| P-05 | Encode/decode the exact 552-byte `SDA2` packet, including flags=0, action-active canonical fields, instance/pre/post equality, full SHA-256 and two unsigned BE u128 public inputs. | `packages/action/v2/{packet,context,packet.test,context.test,typescript/codec}.mjs`; `crates/shieldkit-v2-codec/{src/lib.rs,tests/vectors.rs}`; `circuits/v2-direct/packet-digest.circom` | **Partial, locally exercised.** JS/TypeScript/Rust packet/context codecs completed the local strict sweep: 140,760 packet one-byte replacements per surface, 551/553-byte rejection, and accepted-mutation full-SHA-256/two-BE-u128 checks. No frozen circuit/covenant vector artifact or qualification exists. | P-02–P-04. Pass with committed frozen TS/Rust/circuit/covenant vectors and independently replayable every-byte evidence. |
| P-06 | Enforce exact deposit, transfer, and withdrawal counter/root/reserve deltas, including pre-capacity/pre-live checks and explicit overflow/underflow guards in circuit and covenant. | `packages/action/v2/{transition,transition.test,circuit-model}.mjs`; `circuits/v2-direct/{state,pool-action}.circom`; local tests | **Partial, tracked development-only scaffold.** V2 transition/model and circuit sources exist for all three actions. The BCH covenant/VM enforcement and independent-model/circuit/VM mutation agreement remain unqualified. | P-02, P-05, P-09, P-12. Pass with independent-model, circuit, and VM mutation agreement for each action. |
| P-07 | Implement address, note, nullifier, record encryption, and Faerie resistance exactly as specified: canonical nonzero secrets, subgroup/nonidentity points, domains, 128-byte record, authenticated-tag-bound leaf, and cross-language known-answer vectors. | `packages/action/v2/notes.mjs`; `packages/action/v2/notes.test.mjs`; `crates/shieldkit-v2-codec/src/notes.rs`; `crates/shieldkit-v2-codec/tests/notes.rs`; `circuits/v2-direct/notes.circom` | **Partial, local-only.** JS/Rust constructors/recovery implement `Poseidon(NOTE_LEAF, cm, tag)` and local KATs; `notes.q05-evidence.mjs` completed 152 deterministic checks, including exact BabyJub subgroup-order rejection for both secret roles, 128 authenticated-record byte mutations, and serialized-JSON exclusion of the four secret canary strings. The local wrapper rejects extra child stdout/stderr but does not prove process-memory zeroization, authenticate a compromised parent process, supply an external signature, close an audit, establish a global spent-randomness/reuse registry, or freeze the relation. | P-04, P-05 and cryptographic-audit design freeze. Pass with frozen-profile TS/Rust/circuit KATs, invalid/subgroup/Faerie/reuse tests, canary evidence, zeroization assurance, and dedicated audit closure. |
| P-08 | Implement depth-32 append-only note tree plus depth-32 indexed nullifier tree: sentinel leaves at physical 0/1, full Fr keys, successor links, canonical ordering/insertion, SQLite BE blobs, and no historical-root accumulator. | `packages/pool/v2/`; model/tests; `packages/pool/v2/qualification/`; `artifacts/v2-direct/<profileId>/tree-conformance.json` | **Partial, verified bounded local evidence.** At `b60e91a2914e959d456e31a3ec0f6d3288d3a194`, Q04 verified four independent 25,000-entry histories (100,000 aggregate), 100 close/reopen checkpoints, and 500 deterministic duplicate/ordering/successor-pointer/alias/noncanonical-field rejection probes, with scheduled `Fr=0`/`Fr-1`, zero discrepancies, and zero unexpected accepts. It also binds the stated depth-4 production-Poseidon shared-kernel evidence. Verification: `.codex-build/qualification-b60e91a/q04/q04-SuXwEl/verification.json`, sha256 `daa795ef146c1e0bb06045ab2c2b92797db6d1201209edc912e6dd69f89de41b`. This is not a one-history 100,000-entry run, a formal JavaScript-semantics proof, a state quotient, or a larger-history claim. | P-02, P-04, P-06. The Q04 bounded campaign is passed; P-08 remains partial pending frozen profile/covenant integration and the separate storage/I/O and lifecycle/recovery evidence. Its verified result is limited to the tested range and must not be extrapolated. |
| P-09 | Retain PF11 only as the semantic oracle, and maintain separately identified `PF10-FusedQGenesis` as the provisional local candidate. PF10 has roles `0..4` exec, `5..7` exact MSM, `8` fused final-MSM/identity-aware genesis, `9` terminal, then binding/state/funding at inputs `10..12`. Every carrier requires the real state category; binding verifies packet/public inputs/context/source/successor; state pins the selected roles and successor bundle. | `packages/action/v2/{topology,context,binding-unlock,settlement}.mjs`; `packages/unlock-builder/v2/{pf10-fused-q-genesis,pf10-action-witness,structural-covenants}.mjs`; `packages/kit/v2/cli.mjs`; local/vendored verifier-lane tests; final lock artifacts | **Partial, tracked development-only scaffold.** PF11 remains a rejected semantic oracle. The CLI now fails closed with `PF11_SEMANTIC_ORACLE_ONLY` and permits only the exact PF10 topology/role list before settlement pins, runtime material, or action construction; generic descriptor/oracle tooling retains PF11. PF10 sources are not final covenant locks or BCH-VM qualification, and no final candidate is selected or qualified. | P-02, P-05, P-06, final verifier candidate. Pass with standalone-burn, partial-bundle, mixed-parent, fake-category, duplicate-NFT, minting, omitted-successor rejection in Libauth/BCHN/LeanBCH for the selected candidate. |
| P-10 | Measure and qualify `PF10-FusedQGenesis`; keep PF11 only as the rejected semantic oracle. Preserve the candidate-specific role map, rolling outputs, funding role, tokenlessness, payout, and final dust-derived base values. Hard pass/fail ceilings are serialized transaction <=100,000 bytes, every input unlocking bytecode <=10,000 bytes, and every VM resource <=100%; 90,000/9,500 and all narrower margins are non-blocking telemetry only. | candidate-specific carrier selection; action assembler; genesis builder; tests; `artifacts/v2-direct/<profileId>/carrier-selection.json` | **Partial, measured development-only local Libauth evidence.** `.codex-build/v2-pf10-libauth-qualification/libauth.json` (sha256 `df45748c3c3fb929f3d7568973badfadbe614be68e92c0e508b45535c77c7c10`) records all thirteen inputs accepted for deterministic local fixtures: deposit/transfer 97,852 bytes, withdrawal 97,886 bytes, maximum unlock exactly 10,000 bytes, and peak operation use 96.75%. The artifact itself sets `finalKey`, BCHN, LeanBCH, maintainer benchmark, live-chain provenance, production, and release qualification false. It is telemetry for a provisional candidate, not final qualification. | P-09 and P-12. Pass only with final-artifact measurements, authenticated live-chain parent provenance, full VM evidence, BCHN mempool/mining, LeanBCH, latest unmodified maintainer benchmark, and the selected-candidate report. |
| P-11 | Recompute exact non-circular transaction context from all stated transaction fields, including the selected candidate's frozen input/output roles and counts, excluding unlock/proof/packet/signature/current txid; fix proof/hint width/padding and forbid proof-dependent retry. | `packages/action/v2/{context,context.test,topology,settlement}.mjs`; `circuits/v2-direct/packet-digest.circom`; local vectors | **Partial, tracked development-only scaffold.** The context codec hashes ordered fields and has a topology validator; it deliberately remains parameterized by the non-final candidate. Frozen selected-topology vectors, covenant binding, and deterministic final proof/unlock corpus are missing. | P-05, P-09, P-10. Pass with full field-mutation matrix and deterministic proof/unlock corpus. |
| P-12 | Enforce self-funded one-P2PKH-UTXO fees, exact funding amounts and payout, fresh dust-safe change, default/confirmation fee policy, stale-proof reprove, and post-proof `ALL|UTXOS|FORKID` signing. | `packages/kit/v2/{funding-wallet,funding-signer,transaction-policy,action-lifecycle}.mjs`; `packages/action/v2/settlement.mjs`; corresponding local tests | **Partial, tracked development-only scaffold.** V2 funding, envelope, signing, and stale-action paths exist in local source/tests. They are not accepted/rejected real BCH transaction evidence and do not close the topology/context dependencies. | P-10–P-11, wallet UTXO store. Pass through valid and mutated real BCH transactions accepted/rejected as appropriate. |
| P-13 | Provide durable `prepare/prove/sign/broadcast/resume/abandon/recover` lifecycle, stated journal states, atomic reservations/SQLite WAL FULL/100-block undo, local VM before send, mandatory network gate, reorg/conflict policy, and correct unique-lineage recovery/snapshots. | `packages/kit/v2/`; `packages/pool/v2/{store,recovery-native}.mjs`; `crates/shieldkit-v2-recovery/`; local tests | **Partial, commit-bound local development evidence.** The V2 SQLite store/journal, recovery, network-gate, crash, and reorg/concurrency sources exist. A clean-source Q-06 bundle at `66e3e2273de37cfd8d9c0d179ee8ddd23f4d3b6b` binds the exact source/runtime/install inventory, 10,000 deterministic in-process crash injections, six outer-child SIGKILL boundaries, depth-1/2/10/100 reorgs, 2/4/8/16 sequential sibling campaigns, malicious self-transfer checks, and exact 101-action wipe/replay with zero discrepancies (manifest sha256 `44aab83c4978e2f387f03425619806142415cc6eccaaac29a3edbb7987ee9b9a`). It still does not establish termination inside storage calls, power/filesystem faults, real broadcast/confirmation/signatures/proofs, overlapping or distributed contention, multi-device behavior, or external chain authentication. | P-02, P-05, P-08, P-10–P-12. Pass with reproducible production-representative crash/fault and multi-device campaign artifacts, authenticated live reorg/replay, and no premature commit. |
| P-14 | Expose only the stated V2 CLI flow; `wallet receive` has no faucet; V2 default only after qualification; V1 mutation requires `--protocol v1-legacy` and linkability warning. | `scripts/shieldkit.mjs`; `packages/kit/v2/cli.mjs`; CLI tests; `docs/USER_GUIDE.md` | **Partial, tracked development-only scaffold.** V2 CLI and recovery/fail-closed command tests exist; the implementation labels itself `development-only-unqualified`. No qualification manifest authorizes a V2 default or public flow. | P-12–P-13 and Q-06/Q-07. Pass with command integration tests and qualification manifest gate. |
| P-15 | Publish only the permitted privacy claim and all required nonclaims; prohibit V1→V2 artifact/state migration and keep mainnet outside this plan. | `docs/protocol/v2-direct/PRIVACY_AND_ROLLOUT.md`; `packages/kit/v2/{chain-config,cli}.*`; CLI/descriptor guards and tests | **Partial, local enforcement/documentation exists.** `PRIVACY_AND_ROLLOUT.md` limits public language to the stated passive-observer claim and lists nonclaims; V2 chain config and CLI tests refuse mainnet (`V2_MAINNET_OUTSIDE_PLAN`/`V2_MAINNET_UNAVAILABLE`) and implicit V2 profile/instance migration. This does not establish that every legacy CLI/network path is removed or that any privacy claim is qualified. | P-01, P-13, Q-07. Pass with independent documentation/security review and complete cross-version/mainnet refusal coverage. |

## Phase gates and evidence requirements

| Gate | Requirement | Intended locations | Current status and concrete evidence | Dependencies and objective pass evidence |
|---|---|---|---|---|
| A-01 | Replace/freeze V2 specification, archive batching draft, assign all IDs/domains, and explicitly mark `shielded-action-v2` legacy. | `docs/protocol/v2-direct/{IMPLEMENTATION_PLAN,REQUIREMENT_MATRIX,IDS_AND_DOMAINS}.md`; `docs/archive/v2-batching/`; `packages/profile/legacy.mjs` | **Partial, Phase-A documentation boundary now present.** The V2 Direct plan, identifier/domain registry, and a superseded-batching archive record are present. `packages/profile/legacy.mjs` declares `shielded-action-v2` as V1 legacy and rejects its creation/conversion/migration into V2 Direct; `packages/profile/v2/profile-core.mjs` pins relation/state/packet/address/record/unlock identifiers and rejects `shieldkit-profile-v2-direct` as a profile-core schema. This is current source/test evidence, not an immutable reviewed spec tag or final-artifact evidence. | Must precede the B-01-pre/Q-01-pre freeze and D-01. Existing development-only V2 sources do not retroactively satisfy or invalidate this staged boundary. Pass with a reviewed immutable spec tag and review confirming no active path accepts the legacy relation as V2. |
| A-02 | Strict TS/Rust codecs, byte diagrams/KATs, independent state/tree models, and arithmetic/tree/value/counter proof or model before circuit work. | `packages/action/v2/{state,packet,context,transition,typescript/codec}.mjs`; `crates/shieldkit-v2-codec/`; `docs/protocol/v2-direct/BYTE_LAYOUTS.md`; V2 tree sources/tests | **Partial, local differential evidence.** JS, independent TypeScript, and Rust strict-codec lanes completed per-surface state/packet mutation and digest sweeps against the same machine-readable Q-01 vector. The depth-4 tree qualification now uses production Poseidon, an independently implemented Poseidon oracle, shared production semantic/SQLite kernels, exhaustive occupancy masks, exact symbolic control templates, and an independent Rust checker. The artifacts remain uncommitted and do not provide final circuit/covenant parity, a formal JavaScript-semantics proof, or the required 100,000-entry evidence. | A-01. Pass with an immutable reviewed vector/model set, final circuit/covenant parity, the exact Q-04 campaign, and independently reviewable arithmetic/tree/value/counter evidence. |
| B-01-pre | Freeze the development-key `PoolActionV2Direct` relation, packet, public-input ABI, and selected topology; record constraints/R1CS/prover/VK artifacts and real development-only proofs without implying a final key. | `circuits/v2-direct/`; `packages/action/v2/`; `scripts/v2-b01-pre-freeze.mjs`; development `artifacts/v2-direct/<profileId>/...` | **Partial, fail-closed candidate-freeze verifier implemented; independent review still absent.** The B-01 runner invokes the authoritative 57-artifact PF10 runtime/profile/qualification/Libauth/reproducibility verifier, reruns the commit-bound four-lane Q-01-pre bundle, hashes runtime custody before/after, and emits only `b01-pre-freeze-candidate-awaiting-independent-review` with review, ceremony, final-key, BCH-VM, production, and release claims false. Current PF10 evidence has real witness-valid/proof-verified development-key deposit, transfer, and withdrawal fixtures. No independently reviewed immutable freeze or authorization exists. | A-02. Pass only after reviewers independently re-open the referenced custody bundles, replay the verifier, review relation/packet/public-ABI/topology, and authorize an immutable commit/tag. This is input to D-01, never final-key qualification. |
| B-02-final | Using the final ceremony artifacts, run the latest unmodified verifier benchmark, Libauth, BCHN `testmempoolaccept`, mined BCHN, and LeanBCH; measure every byte/VM/hash item. Block V2 if no candidate passes; do not restore preparation transactions or weaken binding. | `packages/prove/v2-direct/benchmark/`; `artifacts/v2-direct/<profileId>/verification/{maintainer,libauth,bchn-mempool,bchn-mined,leanbch,measurements}.json`; gate test | **Missing final-key external VM foundation; provisional local telemetry only.** PF10's local development-key Libauth hard-limit pass is recorded in P-10. It cannot select or qualify a final candidate. Final-key maintainer, BCHN mempool/mining, LeanBCH, and matching final-artifact evidence are absent. | D-01; frozen B-01-pre/Q-01-pre; final descriptor/runtime; P-09–P-12; maintainer, Libauth, BCHN mempool, mined BCHN, and LeanBCH environments. Pass only when all five independently recorded outputs bind the same immutable final transactions and manifest root. |
| C-01 | Persistent trees, wallet records/reservations/pending/undo/mempool overlay, native Rust scanner, raw-genesis/snapshot recovery, crash-safe split flow, central send gate, 0600 secrets, verified pins, tracked locks and immutable `npm ci`. | P-08/P-13 paths; `crates/shieldkit-v2-recovery/`; `Cargo.lock`; root `package-lock.json`; `artifacts/v2-direct/<profileId>/recovery/` | **Partial, local source/test and commit-bound durability evidence.** Schema v12 store, native recovery, lifecycle, secret-file, and network-gate sources exist. The Q-06 bundle described in P-13 supplies clean-tree/source/runtime/install binding and independently replayed local deterministic campaigns, but explicitly excludes internal-call termination, filesystem/power-loss behavior, network/mined execution, overlapping/distributed contention, and multi-device operation. This does not establish external chain operation, final artifacts, or release qualification. | A-02 for implementation; B-02-final/Q-02 for final-artifact-backed completion. It is not a D-01 prerequisite. Pass with reproducible production-representative stress artifacts and a clean-machine installation transcript. |
| D-01 | After the pre-ceremony relation/packet/ABI/topology and four-implementation codec/digest freeze: phase-2 ceremony with at least five independent contributors, public beacon, transcript verification on two clean machines, reproducible final artifacts on two independent clean hosts. | `artifacts/v2-direct/<profileId>/ceremony/{transcript,beacon,contributors,verify-host-a,verify-host-b,repro-host-a,repro-host-b}.json`; signed manifests | **Missing (hard external gate).** The generic local contribution-chain helper defaults to two participants, but the final D-01 validator requires five contributors and two independent transcript-verification plus two independent reproduction records; no such external evidence exists. | Frozen B-01-pre and Q-01-pre; external contributors and clean hosts. Pass only with five distinct contributor identities, beacon, two independent transcript checks, and matching final hashes. |
| D-02 | Independent audits: protocol/privacy; circuit/encryption/ceremony; covenants/topology; wallet/persistence/recovery/network gate. Unresolved critical/high, applicable medium, skipped mandatory test, synthetic proof, unavailable VM, or nonreproducible artifact blocks qualification. | `scripts/v2-d02-audit-closure.mjs`; `audits/v2-direct/{protocol,circuit,covenants,wallet}/`; `artifacts/v2-direct/<profileId>/qualification/audit-closure.json` | **Missing external audit closure; local fail-closed verifier implemented.** No V2 audit reports or closure artifact were found. | D-01; final Q-01 replay; B-02-final; Q-02/Q-03/Q-07; and the applicable pre-audit Q-05/Q-06 evidence. Explicitly excludes Q-08, Q-09, and any D-02-closure-dependent completion status. Pass only with independent signed reports and closure ledger satisfying the stated blocker policy. |

## Qualification and rollout gates

| Gate | Requirement | Intended test / artifact location | Current status and objective pass evidence |
|---|---|---|---|
| Q-01 | Before D-01, freeze codec/digest conformance across four independent implementations: all state/packet boundaries and byte mutations; state length rejection; category display/order; full SHA-256 two-limb circuit/covenant vectors. After D-01, replay the final artifacts and require byte-identical semantics. | `packages/action/v2/{state,packet,context,strict-codec-qualification,typescript/codec}.test.*`; `crates/shieldkit-v2-codec/tests/`; `circuits/v2-direct/`; `scripts/v2-q01-{commit-bound-evidence,final-artifact-replay}.mjs`; `artifacts/.../qualification/codecs.json` | **Partial; pre-ceremony local evidence and fail-closed final replay verifier implemented, external final replay absent.** At `60a4378184cb367132c913744ccc8b4e9c6bdc12`, the TypeScript, Rust, circuit, and covenant lanes agreed with the JavaScript reference over 32,640 state mutations, 140,760 packet mutations, and 88,727 public-input vectors; the unsigned local bundle manifest sha256 is `b13ae7e0d0f61c0aaf72d8e1b1d5eeeff16421b6d874ec055478de2f193a3b2c`. Each later final source commit requires its own clean-source replay. `v2-q01-final-artifact-replay.mjs` now binds a same-source immutable Q-01-pre bundle to the compiled release root, signed final profile/descriptor/manifest/runtime, exact D-01 result, independently verified relation-source manifest, final R1CS/WASM/zkey/VK/runtime pins, and a fresh four-lane canonical replay. Its injected fixture seam is explicitly test-only and nonqualifying. No compiled final root, authentic D-01 result, signed final artifact set, or post-D-01 success artifact exists. Legacy 80-byte/752-byte tests do not contribute. | Pass before D-01 with a clean committed four-implementation relation/packet vector bundle and reviewed freeze. After D-01, run the public final replay at the identical expected commit/tree and accept only schema `shieldkit-v2-direct-q01-final-artifact-replay-v1` with `q01FinalReplayQualified:true`, `production:false`, and `releaseQualified:false`. |
| Q-02 | 256 final-key proofs and exact selected-topology real transactions per kind (768 total), with exact selected input/output counts, plus proof/packet/state/profile/category/carrier/token/value/role/outpoint/fee/change/payout mutation matrices. | `packages/prove/v2-direct/qualification/proof-corpus.test.ts`; `packages/action/v2-direct/qualification/mutations.test.ts`; `artifacts/.../qualification/{proof-corpus,mutations}.json` | **Missing.** Candidate selection and final-key artifacts are not complete. Pass only with exact raw tx IDs/hashes and agreement in every lane required for that case class: accepted bases require Libauth plus maintainer, BCHN `testmempoolaccept`, BCHN mined inclusion, and LeanBCH; rejected mutations require fresh Libauth plus maintainer, BCHN `testmempoolaccept`, and LeanBCH. BCHN mined evidence is intentionally acceptance-only, and a mined-inclusion envelope for a reject case is invalid rather than negative evidence. |
| Q-03 | Selected-topology covenant attacks: burn, partial bundle, mixed parent, fake category, duplicate NFT, minting authority, omitted successor, altered role counts, and role swaps at every MSM/Miller/terminal seam, including affine and identity branches. | `scripts/v2-q03-{attack-matrix,final-lock-attacks}.mjs` and tests; `packages/action/v2/{binding-unlock,topology,settlement}.test.mjs`; `packages/unlock-builder/v2/{structural-covenants,pf10-fused-q-genesis}.test.mjs`; `artifacts/.../qualification/q03-final-lock-attacks.json` | **Missing external qualification; final fail-closed verifier/test support exists.** The committed exact 219-case attack matrix and final verifier bind final descriptor/manifest/B-02 roots and require replayable Libauth, BCHN-mempool, and LeanBCH rejection pairs. No selected final locks, final corpus, or external lane evidence exists. Pass only with every required attack rejected against final locks in all required lanes. |
| Q-04 | Tree/nullifier stress: exhaustive depth-4, a bounded 100,000-entry deterministic scaling campaign, Fr edge keys, duplicate/order/pointer/alias/noncanonical failures. | `packages/pool/v2/qualification/`; `scripts/v2-q04-{campaign,evidence-verify,history-runner}.mjs`; `.codex-build/qualification-b60e91a/q04/q04-SuXwEl/{evidence,verification}.json` | **Passed, bounded only.** Independent verification at `b60e91a2914e959d456e31a3ec0f6d3288d3a194` reports four fixed-seed 25,000-entry histories (100,000 aggregate), 100 close/reopen checkpoints, and all five probes at each checkpoint (500 total), including scheduled `Fr=0` and `Fr-1`; accepted transitions and membership/nonmembership witnesses matched the independent production-Poseidon oracle, with zero discrepancies and zero unexpected accepts. `verification.json` sha256 is `daa795ef146c1e0bb06045ab2c2b92797db6d1201209edc912e6dd69f89de41b`; bound `evidence.json` sha256 is `b48541fd0971af508f23e14f0c172a1b4c88440b6b8782e7e58381775488de01`. The result also verifies the stated nonformal depth-4 shared-kernel evidence. | This passes only the stated aggregate-four-history bounded Q04 scope. It is not a single 100,000-entry history, larger-history, formal-JavaScript-semantics, state-quotient, or P-08/covenant/lifecycle qualification claim. Q07 storage/I/O and full-pool recovery/performance evidence remain separate. |
| Q-05 | Encryption/privacy and secret safety: invalid/subgroup/malformed/reuse/non-decryptable/Faerie cases and secret-canary scans. | `packages/action/v2/{address,notes}.test.mjs`; `crates/shieldkit-v2-codec/tests/notes.rs`; `artifacts/.../qualification/crypto-safety.json` | **Partial, commit-bound local evidence only.** At `75a604fe602308487dba4e7ba06143d65555bf5a`, the local JS/Rust bundle (manifest sha256 `21f19231011491bbadc733359afbeb64346e9c05b68a895a14943a79f97ac5d9`) covers exact BabyJub subgroup-order rejection for spend and incoming-view secrets, invalid points, malformed records, 128 authenticated-record mutations, wrong recipient, a zero-ephemeral-point record, visible deterministic reuse, and serialized-JSON exclusion of four secret canary strings. It explicitly has no independent audit, authenticated clean-host attestation, global spent-randomness reuse registry, process-memory zeroization proof, frozen final-profile/circuit cross-check, or external qualification status. | Provide applicable pre-audit evidence to D-02. Its final audit closure is a completion condition, not a D-02 prerequisite. Pass with those controls, reproducible frozen KAT/negative evidence, and D-02 closure. |
| Q-06 | Durability/concurrency: 10k crash injections, 1/2/10/100 reorg plus deep wipe/replay, 2/4/8/16 wallet campaigns, parent loss/conflicting siblings/malicious self-transfer; no loss/double commitment/bad reservation/premature commit. | `packages/{kit,pool}/v2/*.{test.mjs,mjs}`; `crates/shieldkit-v2-recovery/tests/replay.rs`; `artifacts/.../qualification/durability.json` | **Partial, commit-bound local deterministic evidence.** The clean-source bundle at `66e3e2273de37cfd8d9c0d179ee8ddd23f4d3b6b` (manifest sha256 `44aab83c4978e2f387f03425619806142415cc6eccaaac29a3edbb7987ee9b9a`) independently verifies 10,000 in-process cases, six outer-child SIGKILL boundaries, the required reorg depths and wallet counts, malicious self-transfer checks, and exact 101-action wipe/replay with zero discrepancies. It explicitly does not cover termination inside storage calls, transaction internals, power loss, filesystem faults, network/mined/real-signature/proof evidence, overlapping/distributed contention, multi-device behavior, or authenticated external-chain reorgs. | Pass with commit-bound independently verified production-representative fault, native recovery, live-chain/reorg, and multi-device campaigns plus invariant reports. |
| Q-07 | Published-machine hard policy gates: serialized transaction <=100,000 bytes, every input unlocking bytecode <=10,000 bytes, and every VM resource <=100%. Narrower byte/resource margins may be reported only as non-blocking risk telemetry. Performance gates: p95 prove <=60s, RSS <=4GiB, apply <=250ms, 100k recovery <=15m/RSS <=2GiB/store <=2.5GiB, and warm tree work within 10% of 1k actions at 100k historical actions; fixed-depth operation counts and cold I/O must be reported separately. | `scripts/v2-q07-final-performance.mjs` and test; `scripts/v2-q07-local-lifecycle-run.mjs` and test; registered portable support under `scripts/v2-q07-*.mjs`; final `artifacts/.../qualification/q07-final-performance.json` | **Implemented fail-closed final verifier; completed local non-chain lifecycle; externally unqualified.** A resumed local bundle verified the exact 100,000-action mix with both JavaScript and Rust over a 670,291,065-byte corpus (corpus sha256 `aa39af48d1690aa9e45885e52816b11b95ff08820dbe26625eac215777819fde`; manifest sha256 `4c21368ec6e7385b095538a361c1c42fd7114c09aa5f5358bcee6460e37ef9e7`). It is explicitly interrupted-run-resume, local-only, non-chain, non-final, non-published-machine, and not Q07-qualified. No final key, 768 proof/transaction corpus, BCHN, LeanBCH, maintainer benchmark, signed published-machine evidence, full final recovery result, warm-tree ratio, or true cold-I/O evidence exists. | Pass with final frozen artifacts, the authoritative verifier opening/hashing/replaying the exact action corpus and raw samples, byte/VM breakdown, true cold-I/O evidence, signed machine manifest, completed full-pool 100k scaling/recovery evidence, and all independent VM lanes. |
| Q-08 | Clean-machine qualification: verify signed/pinned artifacts, `npm ci`, wallet, out-of-band funding, sync, one deposit/transfer/withdrawal, erase state, recover, and spend the recovered note. Each approved host runs the one-shot `scripts/v2-clean-machine-qualification.mjs` under a compiled release-root role and emits an Ed25519-signed host envelope. Every action carries SHA-256-pinned references to exact local Libauth evidence and signed maintainer, BCHN mempool, mined BCHN, and LeanBCH envelopes. The independent `scripts/v2-q08-pair-qualification.mjs` replays every referenced artifact before accepting the matching host-A/host-B envelopes and their final root/profile/descriptor/runtime/source pins. Boolean lane claims are rejected. | `scripts/v2-q08-lane-evidence.mjs`; `scripts/v2-clean-machine-qualification.mjs`; `scripts/v2-q08-pair-qualification.mjs`; final host evidence bundles and `artifacts/.../qualification/q08-pair.json` | **Missing (external execution gate); local fail-closed verifier implemented.** Authentic test-only evidence covers exact local VM replay, four independently signed external roles, subject/signature/hash/path tampering, BCHN rejection/mined-proof drift, and distinct-run enforcement. No two real clean-host bundles or pair artifact are present. Pass only with two independently run, release-root-authorized host transcripts and the independently replayed pair bound to the final manifest and raw transaction evidence. |
| Q-09 | Chipnet rollout: high-capacity instance; 30-day soak with >=1,000 direct settlements; separate 32-live-note playground with no sponsor/faucet, admission-33 pre-proving rejection, withdraw/refill/recover/contention; publish only after final-key and clean-machine gates. | `scripts/v2-chipnet-soak.mjs`; `scripts/v2-q08-pair-qualification.mjs`; final `artifacts/.../chipnet/{instance,settlements,soak,playground}.json` | **Partial, fail-closed local validator; hard external time/network gate remains.** `v2-chipnet-soak.mjs` re-verifies the root-bound Q-02 corpus and replayed Q-08 pair artifact before evaluating chain evidence, and requires separately signed high-capacity and exactly-32-note descriptors. It validates raw headers, exact-count Merkle proofs, distinct Ed25519 observer keys, settlement lineage, the 32-note admission/refill/recovery/contention sequence, fresh all-input Libauth evidence for every prepared contention candidate, and identical carrier/binding/state prevouts across winner and losers while allowing only funding to differ. It rejects summaries, fixtures, mocks, synthetic timestamps, gaps, duplicate states, mainnet, sponsor/faucet/batching, development runtime material, opaque txids, and unconfirmed records. Its test seam is `test-only-nonqualifying`. No external evidence exists. |

## Dependency order and hard stops

```text
A-01 specification/legacy quarantine
  -> A-02 independent codecs + models
    -> B-01-pre relation + packet + ABI + selected-topology freeze
      -> Q-01-pre committed four-implementation codec/digest freeze
        -> D-01 final ceremony + two transcript checks + two-host reproduction
          -> Q-01 final-artifact replay
          -> B-02-final VM foundation
            -> {Q-02 final-key corpus, Q-03 final-lock attack corpus,
                Q-07 published-machine evidence}

{Q-01 final-artifact replay, B-02-final, Q-02, Q-03, Q-07,
 applicable pre-audit Q-05/Q-06 evidence}
  -> D-02 independent audit closure
    -> Q-08 two clean hosts
      -> Q-09 30-day Chipnet soak and playground
```

`C-01`, Q-04, and local Q-05/Q-06 development work may proceed after A-02 in
parallel with B-01-pre. Applicable final-artifact-backed Q-05/Q-06 evidence is
an audit input after D-01/B-02-final, while their D-02-closure-dependent status
is never an input to D-02. They are not ceremony prerequisites.

Hard stops are: no final-key proof corpus, Q-02/Q-03/Q-07 result, or
release-qualified V2 artifact until D-01 and B-02-final; no release
qualification until D-02; no public playground until Q-02 and Q-08; no
rollout completion until the 30-day, >=1,000-settlement Q-09 record. A failed
carrier candidate blocks V2 Direct rather than permitting a preparation
transaction or weakened binding.

## Contradictions and migration hazards to resolve before implementation

| Conflict | Current repository evidence | Required resolution |
|---|---|---|
| State encoding | Legacy sources use an 80-byte `SHST` commitment; tracked development-only V2 sources implement 128-byte `SKS2` with no secondary state commitment. | Keep V1 codec isolated; retain P-02's V2 codec/vector boundary and prohibit interpreting either encoding as the other. |
| Packet encoding | Legacy sources use a 752-byte `SCAR` packet; tracked development-only V2 sources implement a 552-byte `SDA2` packet. | Retain P-05's V2 codec/vector ABI; recovery and covenant parsers must version-dispatch and refuse cross-version decoding. |
| Relation/setup identity | `shielded-action-v2` remains V1 legacy research; tracked V2 Direct documentation assigns `shieldkit-pool-action-v2-direct`, while only ignored development setup artifacts exist. | A-01 legacy quarantine; no label reuse or profile relabeling. |
| Transaction topology | Existing sources include `prep.mjs`, preparation outputs, and legacy ten-role settlement testing. PF11 is only a rejected semantic oracle; the operational CLI explicitly rejects PF11 with `PF11_SEMANTIC_ORACLE_ONLY` and requires the exact PF10 role list before pins/runtime/action construction. Provisional PF10 local transactions use thirteen inputs, thirteen deposit/transfer outputs, and fourteen withdrawal outputs. | P-01/P-10 topology policy must reject old construction paths; the PF10 local layout is not a release-frozen descriptor until all qualification gates pass. |
| Tip selection | Legacy `packages/kit/state-tip.mjs` tests prefer higher `actionSequence`; tracked development-only V2 store/recovery sources implement a separate descriptor-bound recovery path, but its unique-lineage qualification remains open. | Keep V1 isolated; exercise competing disconnected candidates through P-13's V2 scanner qualification. |
| Ceremony threshold | Existing local ceremony tests accept >=2 participants; V2 final ceremony requires >=5 independent contributors plus beacon and two-host verification. | D-01 is external/final only; local simulations remain development-only and cannot be promoted. |
| Current operational claims | The portable suite includes local V2 source tests; separate local-covenant/local-verifier suites require ignored development artifacts, and external suites are classified separately. | Do not infer Chipnet, final-key, VM, audit, or production status from any local suite. |

## Update discipline

For each row, attach the exact command, git revision, environment/tool versions,
input manifest hashes, raw output hashes, and pass/fail result before changing its
status. “Existing” may become “passed” only when the listed objective evidence exists
for the final V2 identifiers and artifacts. Qualification remains closed until every
Q/D hard gate is independently satisfied.
