# Local explainer webpage

Static system explainer for ShieldKit (no build step).

## Serve

```bash
# from repo root
python3 -m http.server 8765 --directory local-explainer-webpage
```

Open **http://127.0.0.1:8765/learn-about-this-system.html**  
(`index.html` redirects there.)
