# `dev/` — not the product surface

Everything here supports **protocol freeze, gates, and lab history**.

App developers: start at the **repo root README** and **`packages/`**.

| Path | Contents |
|------|----------|
| `protocol/` | AGENTS, policy (g0), circuits, evidence, spec |
| `research/` | Android, provenance, old scripts/docs, experiments |

Do not treat `dev/` as the SDK API or onboarding path.
