
.cache/
.codex-artifacts/
coverage/
dist/
.grok/
HANDOVER.md
# large profile artifacts (not in git)
.local/
# Local handover / secrets — never commit without explicit user authorization
*.log
node_modules/
**/*private*key*
# Project-local worktrees only — never create worktrees outside this repo
**/secrets/**
target/
use-chipnet-demo-pool/bundle/
use-chipnet-demo-pool/state.json
**/*.wif
.worktrees/
